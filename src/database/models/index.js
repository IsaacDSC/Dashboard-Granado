const { Account } = require('./account')
const { Events } = require('./events.models')
const { Registered } = require('./register.models')
const { RegisteredEvents } = require('./registerEvents.models')



module.exports = { Account, Events, Registered, RegisteredEvents }
